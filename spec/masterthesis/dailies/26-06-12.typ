

./26-06-13.typ
== Fragen
- Wie mach ich instantierung von Lets?
  - Einfach mit paar Typ regeln?

== Misc
- Anscheinend ist der Principality Beweis sehr arsch
- An sich kann ich mein ML-system ja einfach auf
- »As we can not predict if a label variable will introduce shadowing after instantiation«

== Tabular Data
- Der _Tailcheck_ wird verwendet, damit label nur in tail position instantiiert werden
- Die _Instantiierung_ wird verwendet, weil wir _let-poly_ haben
- Die _Row-equivalance_ wird zu equivalence (≡) erweitert

